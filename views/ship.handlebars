<h1 style="text-align: center;"><a href="/">CoHo Architectural Design</a></h1>

{{#if success}}
    <h2>Shipment successfully processed.</h2>
    {{#each shipmentDetails}}
        <h3>Ordered product: {{productId}} Qty: {{quantity}} Previous inventory: {{previousInventory}} New inventory: {{updatedInventory}} </h3>
    {{/each}}
    <h1><a href="/">Back to Main Page</a></h1>
{{else}}
    <h2>{{error}}</h2>
    <h1><a href="/">Back to Main Page</a></h1>
{{/if}}