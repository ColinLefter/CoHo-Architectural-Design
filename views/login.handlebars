<!DOCTYPE html>
<html lang="en">
<body class="dark:bg-black"> <!-- Set the background of the entire page to black -->

  <div class="mt-10 sm:mx-auto sm:w-full sm:max-w-md bg-white text-black dark:bg-black text-white">
    <h2 class="text-center text-2xl font-bold leading-9 tracking-tight text-gray-900 dark:text-white">Sign in to your account</h2>

        {{#if loginMessage}}
            <p class="mt-4 text-center text-sm text-red-600">{{loginMessage}}</p>
        {{/if}}

        <form class="mt-6 space-y-6" action="/validateLogin" method="post">
            <div>
                <label for="username" class="block text-sm font-medium leading-6 text-gray-900 dark:text-white">Username</label>
                <div class="mt-2">
                    <input id="username" name="username" type="text" required class="block w-full rounded-md bg-black border border-white text-white py-1.5 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm">
                </div>
            </div>

            <div>
                <label for="password" class="block text-sm font-medium leading-6 text-gray-900 dark:text-white">Password</label>
                <div class="mt-2">
                    <input id="password" name="password" type="password" required class="block w-full rounded-md bg-black border border-white text-white py-1.5 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm">
                </div>
            </div>

            <div>
                <button type="submit" class="flex w-full justify-center rounded-md bg-indigo-600 dark:bg-white dark:text-black px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm hover:bg-indigo-500 dark:hover:bg-indigo focus:outline-none focus:ring-2 focus:ring-indigo-500">Log In</button>
            </div>
        </form>

        <p class="mt-10 text-center text-sm text-gray-500 dark:text-white">
            Not a member?
            <a href="/createAccount" class="font-semibold leading-6 text-indigo-600 hover:text-indigo-500">Register right now.</a>
        </p>
  </div>
</body>
</html>
